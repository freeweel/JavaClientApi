/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.3/userguide/java_library_plugin.html
 */

plugins {
  id "java"
  id "eclipse"
}
eclipse {
  classpath {
    downloadJavadoc = true
    downloadSources = true
  }
}


// Apply the java-library plugin to add support for Java Library
apply plugin: 'java-library'

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter {}
    mavenCentral()
    mavenLocal()
    maven {url 'http://developer.marklogic.com/maven2/'}
    maven {url 'https://repo.maven.apache.org/maven2/'}
}

//  compile 'com.marklogic:marklogic-client-api:4.0.1'  // �other application dependencies

dependencies {
  
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    implementation 'com.google.guava:guava:23.0'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
    
    // MarkLogic Java Client API (5.4.0 required for compatibility with Data Hub 5.5.1)
    compile group: 'com.marklogic', name: 'marklogic-client-api', version: '5.4.0'

    // MarkLogic Data Hub API
    compile group: 'com.marklogic', name: 'marklogic-data-hub', version: '5.5.1'

    // Needed for SLF4
    compile group: 'org.slf4j', name:'slf4j-api', version: '1.7.2'
    compile group: 'ch.qos.logback', name:'logback-classic', version: '1.0.9'
    compile group: 'ch.qos.logback', name:'logback-core', version: '1.0.9'

    // This prevented a warning message
    compile group: 'com.fasterxml.jackson.module', name:'jackson-module-kotlin', version: '2.13.0+'

    // Apache commons IO
    compile group: 'commons-io', name: 'commons-io', version: '2.8.0'

    // Amazon SDK for S3
    implementation platform('com.amazonaws:aws-java-sdk-bom:1.11.1000')
    implementation 'com.amazonaws:aws-java-sdk-s3'

    // https://mvnrepository.com/artifact/com.jayway.jsonpath/json-path
    implementation group: 'com.jayway.jsonpath', name: 'json-path', version: '2.6.0'
}




